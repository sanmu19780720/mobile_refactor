# 数据库连接（保持与原 PHP config.php 一致）
DB_HOST=120.78.239.45
DB_PORT=3306
DB_NAME=my_schema
DB_USER=root
DB_PASS=xiangJIANG2022.

# JWT 配置
JWT_SECRET=change-me-to-a-long-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=720

# CORS 允许的前端来源（逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 本地账号（JSON 数组：用户名/明文密码/真实姓名/角色）
# 与原 local_users.php 等价；首次启动时明文会自动哈希校验
LOCAL_USERS_JSON=[["管群","Kayla2025","管群","admin"]]
